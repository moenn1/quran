# QuranKit CLI

Typer-based command-line interface for QuranKit.

Current scope:

- configuration storage for remote API and local SQLite modes
- initial `config show` and `config set` commands
- test coverage for settings persistence and backend selection
