# QuranKit Embeddings Package

This package is reserved for optional vector-search and embedding integration work.

## Guardrails

- Semantic search must be described as textual similarity only.
- Vector search must never be presented as tafsir, fatwa, or religious ruling.
- Enable embeddings only after attribution and privacy rules are documented and testable.
