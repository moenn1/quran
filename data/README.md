# QuranKit Data Workspace

This directory is reserved for local development data artifacts, import inputs, and reproducible validation outputs.

## Rules

- Do not commit private user data.
- Do not commit Quran imports without verified attribution and integrity checks.
- Prefer reproducible scripts in `scripts/` over manual edits to stored artifacts.
